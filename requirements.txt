# Root-level meta-requirements.
#
# The actual dependencies live with the code that uses them:
#   - safety_nlp_pipeline/requirements.txt   core NLP pipeline (fetch/train/eval/RAG/report/monitor)
#   - aviation-safety-app/backend/requirements.txt   FastAPI backend
#   - aviation-safety-app/frontend/package.json     React + TypeScript dashboard
#
# This file is kept for reference and for the root-level helper scripts:
#   - make_report.py    -> builds Aviation_NLP_Pipeline_Report.docx (needs python-docx)
#   - app.py            -> legacy Textual terminal UI (needs textual)
#
# Install only what you actually need (the one-click launcher does this for you):
#   python start.py
#     -> installs safety_nlp_pipeline/requirements.txt + backend/requirements.txt + npm

# Helper scripts at the repo root
python-docx>=1.1     # make_report.py builds the DOCX report
textual>=8.2         # legacy TUI (python app.py) - optional, kept for reference

# Also mirrored here for convenience (the launcher installs the per-package files):
pandas>=2.2
scikit-learn>=1.5
datasets>=3.0
joblib>=1.4
nltk>=3.8
pypdf>=4.0
requests>=2.31
matplotlib>=3.8
seaborn>=0.13
jinja2>=3.1
