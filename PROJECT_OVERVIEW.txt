================================================================================
SAFETY NLP PIPELINE — PROJECT OVERVIEW
Aviation & Power-Grid Incident Text Classification + RAG Explainability
================================================================================

TABLE OF CONTENTS
  1. What this project is
  2. Project goals
  3. Data sources
  4. Pipeline architecture
  5. Web Dashboard (React + FastAPI) - PRIMARY INTERFACE
  6. Fault tolerance & idempotency
  7. Headless CLI
  8. Configuration
  9. Dependencies
 10. Project structure
 11. How to run
 12. Results
 13. Research gaps addressed
 14. Git history
 15. Extras
 16. Real-time monitoring & alerting

-------------------------------------------------------------------------------
1. WHAT THIS PROJECT IS
-------------------------------------------------------------------------------
A production-grade, HEADLESS NLP pipeline that runs end-to-end with ZERO user
intervention (`python main.py`) and produces a comprehensive, self-contained
HTML report (model metrics, confusion matrix, RAG evidence examples, data
quality insights).

**NEW**: A modern, cross-platform **React + FastAPI Web Dashboard** (`aviation-safety-app/`)
provides full visual control over all NLP processes with rich interactive
visualizations, real-time progress tracking, and live alert monitoring.

The pipeline is modular, reproducible and fault tolerant: if a data source is
temporarily unavailable it falls back to cached data or gracefully skips that
domain.

It covers two safety-critical domains:
  - AVIATION   : NASA ASRS (Aviation Safety Reporting System) incident reports
  - POWER GRID : NERC (North American Electric Reliability Corporation) event
                  analysis reports

It performs, end to end:
  1. Live data collection (fetch / refresh, idempotent)
  2. Text preprocessing (NLTK)
  3. Classification model training (TF-IDF + SGD/log-loss with GridSearchCV)
  4. Evaluation (classification report + confusion matrix + plots)
  5. Batch RAG explainability (semantic evidence retrieval for the test set)
  6. Self-contained HTML report generation (Jinja2)
  7. **Web Dashboard** (React + FastAPI) - Full visual control interface
  8. **Real-time monitoring & alerting** (src/monitor.py): continuous service

-------------------------------------------------------------------------------
2. PROJECT GOALS
-------------------------------------------------------------------------------
  - Build a professional, observable, repeatable NLP solution over real public
    safety data that produces tangible outputs (HTML report + web dashboard).
  - Zero user intervention: one command runs the entire pipeline.
  - Every prediction must be explainable: RAG retrieves the top-3 most similar
    historical reports as evidence (an auditable explanation by example).
  - Model robustness: cross-validation (StratifiedKFold) + hyperparameter
    tuning (GridSearchCV over alpha and class_weight).
  - Fault tolerance: per-domain graceful fallback to cached data.
  - A "keyless" data assistant (no LLM, no API key) that audits data quality
    and safety criticality with pandas.
  - **Full web-based control**: All NLP processes controllable from browser.

-------------------------------------------------------------------------------
3. DATA SOURCES
-------------------------------------------------------------------------------
The dataset is collected LIVE from public sources every run (or reused from a
cache) — nothing is pre-baked into the repository.

  DOMAIN      | SOURCE                                   | WHAT IS DOWNLOADED
  ------------+------------------------------------------+--------------------
  Aviation    | NASA ASRS via Hugging Face datasets-server| 2,000 real incident
              | (elihoole/asrs-aviation-reports)         | reports with expert
              |                                          | anomaly labels from
              |                                          | Events_Anomaly
  Power grid  | NERC Event Analysis reports (public PDFs)| 12 real grid-incident
              |                                          | post-mortems
              |                                          | (Northeast 2003
              |                                          | blackout, Hurricane
              |                                          | Sandy, San Fernando
              |                                          | disturbance, ...)

Aviation: streamed row-by-row from the Hugging Face datasets-server API in
paginated batches of 100. Narrative text from "Report 1_Narrative", expert
anomaly labels from "Events_Anomaly" (multi-label strings split to the primary
category; rare categories bucketed as "Other").

Power grid: 12 public NERC PDFs are downloaded (cached in data/raw/), text is
extracted with pypdf and split into incident narratives with NLTK sentence
tokenization (~5 sentences per chunk).

Both sources are cleaned (dedupe, drop empty labels, drop very short rows,
collapse whitespace) and merged into one Domain-tagged dataset saved at:
    data/real_safety_dataset.csv
(~3,700 cleaned reports: 2,000 aviation + ~1,700 power-grid narratives)

The 12 NERC reports (from config.py):
  - Power Grid - Northeast Blackout 2003
  - Power Grid - Hurricane Sandy
  - Power Grid - Hurricane Harvey
  - Power Grid - Hurricane Irma
  - Power Grid - San Fernando Disturbance
  - Power Grid - January 11 Oscillation Event
  - Power Grid - South Central Cold Weather
  - Power Grid - Solar PV Disturbance 2018
  - Power Grid - Northeast Snowstorm 2011
  - Power Grid - Arizona Southern California Outage
  - Power Grid - IBR Disturbances Western Interconnection
  - Power Grid - January 2025 Arctic Events

-------------------------------------------------------------------------------
4. PIPELINE ARCHITECTURE
-------------------------------------------------------------------------------
  data/real_safety_dataset.csv   (NASA ASRS + NERC, ~3,700 domain-tagged reports)
        |
[1/6] FETCH      Live-download ASRS + NERC, clean, merge by domain -> CSV
        |
[2/6] PREPROCESS NLTK tokenize -> stopword removal -> lemmatize (per document)
        |
[3/6] TRAIN      TF-IDF (bigrams, max_features=5000) + SGD (log-loss) with GridSearchCV
        |
[4/6] EVALUATE   classification report + confusion-matrix heatmap + class distribution plot
        |
[5/6] RAG        batch explainability: top-3 most similar historical reports per prediction
        |
[6/6] REPORT     self-contained HTML report with all results
        |
[WEB] FASTAPI    REST API wrapping all pipeline functionality
        |
[WEB] REACT      Cross-platform dashboard (Web)
        |
[MON] MONITOR    Real-time incident monitoring & alerting

STEP DETAILS
  1. DATA_FETCHER (src/data_fetcher.py)
     - _fetch_aviation(): streams ASRS rows from datasets-server; keeps rows
       with a narrative AND a non-empty anomaly label; top-15 categories kept,
       the rest merged into "Other".
     - _fetch_power(): downloads each NERC PDF, parses with pypdf, cleans
       header/footer noise lines, sentence-tokenizes with NLTK and builds
       ~5-sentence narrative chunks tagged with the event label.
     - Graceful SSL fallback (some corporate / Python CA stores fail).
     - Merges both domains into one "domain"-tagged DataFrame and saves CSV.

  2. PREPROCESSOR (src/preprocessor.py)
     - NLTK: tokenize -> lowercase -> keep alphanumeric -> remove English
       stopwords -> WordNet lemmatize. NLTK corpora auto-download on first run.

  3. TRAINER (src/trainer.py)
     - Per-document NLTK preprocessing with progress logging.
     - Stratified train/test split (80/20, random_state=42).
     - TfidfVectorizer: stop_words="english", max_features=5000,
       ngram_range=(1, 2) — bigrams capture domain terms like "gear down" /
       "load shed" / "transmission line".
     - GridSearchCV over SGDClassifier(loss="log_loss") hyperparameters
       (alpha in [1e-5, 1e-4, 1e-3] x class_weight in [None, "balanced"])
       using StratifiedKFold(3) and f1_weighted scoring. The test set is never
       used for tuning.
     - Saves best model + vectorizer (joblib) and training_config.json.

  4. EVALUATOR (src/evaluator.py)
     - Vectorizes the test set and predicts with the best model.
     - Per-class classification report (precision/recall/f1/support).
     - Confusion-matrix heatmap (normalised) + top-N class-distribution bar
       chart, saved as PNGs under data/plots/.
     - Persists classification_report.txt/.csv and metrics.json for the
       dashboard.

  5. RAG_EXPLAINER (src/rag_explainer.py)
     - batch_rag(): samples test reports; for each, predicts the class and
       computes cosine similarity against ALL training narratives in batches;
       returns the top-3 most similar historical reports as evidence.
     - build_index() + explain_text(): fast single-query retrieval for the
       interactive dashboard (the corpus matrix is precomputed once).

  6. REPORT_GENERATOR (src/report_generator.py)
     - Jinja2 template builds a self-contained HTML page with: project summary
       + dataset statistics, data-quality audit, best hyperparameters,
       per-class metrics table, embedded confusion-matrix and class
       distribution plots (base64), and RAG examples with similarity bars.
     - Saves as reports/pipeline_report.html — opens in any browser.

  7. ANALYST (src/analyst.py)
     - Keyless data assistant (quality / safety / classes / risk scanning).

  8. MONITOR & ALERTER (src/monitor.py) - see section 16.

  9. WEB DASHBOARD (aviation-safety-app/) - PRIMARY INTERFACE
     - FastAPI backend exposing all pipeline functionality as REST API
     - React + TypeScript frontend with Tailwind CSS
     - 6 dashboard pages: Overview, Model Performance, RAG Explorer,
       Data Assistant, Live Alerts, System Control
     - Recharts visualizations: bar charts, pie charts, progress bars
     - Real-time pipeline progress tracking via polling
     - WebSocket live log streaming
     - Full pipeline control: run, configure, monitor, view logs

-------------------------------------------------------------------------------
5. WEB DASHBOARD (React + FastAPI) - PRIMARY INTERFACE
-------------------------------------------------------------------------------
Location: `aviation-safety-app/`

### Backend (FastAPI)
- **main.py**: All API endpoints for 6 dashboard pages + system control
- **ml_service.py**: ML pipeline wrapper with PipelineProgress tracking
- **schemas.py**: Pydantic models for request/response validation
- **config.py**: Pydantic settings with environment variable support

### Frontend (React + TypeScript + Vite)
- **Pages**: Overview, Model Performance, RAG Explorer, Data Assistant, Live Alerts, System Control
- **Charts**: Recharts-based BarChart, PieChart, LineChart, ProgressBar components
- **UI Components**: Card, Table, Badge, MetricCard, StatGrid, Input, Button
- **State**: Zustand for global state, React hooks for data fetching
- **Styling**: Tailwind CSS with custom cream/brown theme

### Dashboard Pages

| Page | Features |
|------|----------|
| **Overview** | Domain distribution pie, top classes horizontal bars, metric cards, narrative stats |
| **Model Performance** | Per-class P/R/F1 grouped bars, support chart, confusion matrix image, detailed table with F1 progress bars |
| **RAG Explorer** | Sample narratives, top-K selector, prediction card, evidence cards with similarity bars |
| **Data Assistant** | Quick analysis buttons, natural language input, history with copy, example queries |
| **Live Alerts** | Risk level pie chart, source bar chart, filterable table, expandable evidence, timeline |
| **System Control** | Service status badges, pipeline stage progress bars, live logs, pipeline options |

### Running the Dashboard
```bash
# Prerequisites: Run core pipeline first to generate artifacts
cd safety_nlp_pipeline && python main.py

# Terminal 1: Backend
cd aviation-safety-app/backend
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords'); nltk.download('wordnet'); nltk.download('punkt_tab')"
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Terminal 2: Frontend
cd aviation-safety-app/frontend
npm install && npm run dev
# Opens at http://localhost:5173
```

-------------------------------------------------------------------------------
6. FAULT TOLERANCE & IDEMPOTENCY
-------------------------------------------------------------------------------
  - If data/real_safety_dataset.csv exists → fetch **skipped** (idempotent)
  - One domain fails → falls back to cached rows for that domain
  - Both fail → cached dataset reused (if present)
  - NERC PDFs cached in data/raw/ → fast re-fetches
  - Legacy column names (Narrative, human_factors_groundtruth, Domain) auto-normalised

-------------------------------------------------------------------------------
7. HEADLESS CLI
-------------------------------------------------------------------------------
  python main.py --fetch-only       (not implemented; see flags)
  python main.py --no-fetch         # skip fetching, require cached CSV
  python main.py --force-refresh    # re-download data
  python main.py --no-rag           # skip RAG explainability
  python main.py --samples N        # RAG test samples (default 100)
  python main.py --monitor          # start the real-time monitor after training
  python main.py --poll N           # monitor poll interval in seconds (default 60)

  # monitor standalone:
  python -m src.monitor             # continuous loop (watches + live feeds)
  python -m src.monitor --once --no-api   # single scan (CI / demos)

Each src module is also runnable directly (e.g. python src/data_fetcher.py).

-------------------------------------------------------------------------------
8. CONFIGURATION (config.py)
-------------------------------------------------------------------------------
  Data      : NROWS_AVIATION=2000, TOP_CATEGORIES=15, NERC_PDFS, PDF_CACHE
  Modeling  : MAX_FEATURES=5000, NGRAM_RANGE=(1,2), TEST_SIZE=0.2,
              CV_FOLDS=3, GRID_ALPHAS, GRID_CLASS_WEIGHTS, SGD_MAX_ITER=1000
  RAG       : RAG_TOP_K=3, RAG_BATCH=200, RAG_N_SAMPLES=100,
              RAG_EVIDENCE_SNIPPET=170
  Monitor   : WATCH_DIR=new_incidents/, ALERT_LOG_PATH=data/alerts.csv,
              MONITOR_POLL_SECONDS=60, MONITOR_ALERT_SNIPPET=200,
              NTSB_API_URL + NTSB_POLL_SECONDS=3600,
              UKPN_API_URL + UKPN_POLL_SECONDS=60,
              ALERT_HIGH_MIN_CUSTOMERS=100

-------------------------------------------------------------------------------
9. DEPENDENCIES
-------------------------------------------------------------------------------
The actual dependency lists live with each component:
  - safety_nlp_pipeline/requirements.txt          core NLP pipeline
  - aviation-safety-app/backend/requirements.txt  FastAPI backend
  - aviation-safety-app/frontend/package.json    React + TypeScript dashboard

Summary:
  Core Pipeline (safety_nlp_pipeline/):
    pandas>=2.2, scikit-learn>=1.5, datasets>=3.0, joblib>=1.4,
    nltk>=3.8, pypdf>=4.0, requests>=2.31, matplotlib>=3.8,
    seaborn>=0.13, jinja2>=3.1
    
    Optional (legacy Streamlit dashboard):
    streamlit>=1.37
    
    Optional (legacy Textual terminal UI):
    textual>=8.2

  Backend API (aviation-safety-app/backend/):
    fastapi==0.115.0, uvicorn[standard]==0.34.0, pydantic==2.10.6,
    pydantic-settings==2.9.0, python-multipart==0.0.9, python-dotenv==1.0.1,
    joblib==1.4.2, pandas==2.2.3, numpy==2.1.3, scikit-learn==1.6.1,
    scipy==1.14.1, requests==2.32.3, pypdf==5.1.0, nltk==3.9.1,
    httpx==0.28.1, websockets==13.1, watchfiles==1.0.5

  Frontend (aviation-safety-app/frontend/):
    react@18.3.1, react-dom@18.3.1, react-router-dom@6.26.2,
    axios@1.7.7, recharts@2.12.7, lucide-react@0.441.0,
    clsx@2.1.1, tailwind-merge@2.5.2, date-fns@4.1.0,
    react-hot-toast@2.4.1, zustand@5.0.0, tailwindcss@3.4.13,
    typescript@5.6.2, vite@5.4.8

  Helper scripts at the repo root:
    python-docx>=1.1   (make_report.py - builds Aviation_NLP_Pipeline_Report.docx)
    textual>=8.2       (app.py - legacy Textual terminal UI, optional)

-------------------------------------------------------------------------------
10. PROJECT STRUCTURE
-------------------------------------------------------------------------------
Aviation_NLP_Project/
|-- start.py, start_app.bat / .sh / .ps1   one-click launchers
|-- make_report.py                          builds Aviation_NLP_Pipeline_Report.docx
|-- app.py, pipeline/                       legacy Textual TUI (kept as developer tool only)
|-- Aviation_NLP_Pipeline_Report.docx       generated DOCX report (run make_report.py)
|-- Research Document AI Applications...docx supporting research material
|-- _research_extract.txt, _tables_extract.txt  text/table extracts from the research doc
|-- aviation-safety-app/                    Web Dashboard (React + FastAPI)
|   |-- backend/
|   |   |-- main.py               FastAPI endpoints
|   |   |-- ml_service.py         ML wrapper + progress
|   |   |-- config.py             Settings (incl. Ollama)
|   |   |-- schemas.py            Pydantic models
|   |   |-- ollama_service.py     Ollama client with domain-constrained chat
|   |   |-- suggestions.py        Auto-generated safety suggestions for alerts
|   |   |-- .env.example          Environment variable template
|   |   `-- requirements.txt
|   |-- frontend/
|   |   |-- src/
|   |   |   |-- components/
|   |   |   |   |-- charts/       Recharts components (Bar, Pie, Line, Progress)
|   |   |   |   |-- layout/       Sidebar, Header, Layout, StatusBar
|   |   |   |   `-- ui/           Card, Table, Badge, MetricCard, Input, Button
|   |   |   |-- pages/            Overview, Performance, RAG, Assistant, Alerts, System
|   |   |   |-- hooks/            useApi, useOllamaStatus hooks
|   |   |   |-- store/            Zustand state
|   |   |   |-- api/              Axios client
|   |   |   |-- types/            TypeScript types
|   |   |   `-- utils/            Helpers
|   |   |-- package.json
|   |   |-- vite.config.ts
|   |   |-- tailwind.config.js
|   |   `-- tsconfig.json
|   `-- README.md
|
|-- safety_nlp_pipeline/                    Core Python Pipeline
|   |-- src/
|   |   |-- data_fetcher.py
|   |   |-- preprocessor.py
|   |   |-- trainer.py
|   |   |-- evaluator.py
|   |   |-- rag_explainer.py
|   |   |-- analyst.py
|   |   |-- monitor.py
|   |   `-- report_generator.py
|   |-- main.py
|   |-- config.py
|   |-- MONITOR_GUIDE.md
|   |-- app_streamlit.py                    Legacy Streamlit (deprecated; use React web app)
|   |-- new_incidents/                      Drop-in folder for new CSV/TXT reports
|   `-- requirements.txt
|
|-- PROJECT_OVERVIEW.txt                    This file
|-- README.md                               Main project README
`-- .gitignore

--------------------------------------------------------------------------------
11. HOW TO RUN
--------------------------------------------------------------------------------
  # 0. ONE-CLICK LAUNCHER (recommended) - runs the pipeline if needed, then
  #    starts backend + frontend and opens the dashboard in your browser.
  python start.py                    # cross-platform Python launcher
  ./start_app.sh                     # Linux / macOS
  ./start_app.ps1                    # Windows PowerShell
  start_app.bat                      # Windows cmd

  # 1. Core pipeline (generates all artifacts)
  cd safety_nlp_pipeline
  pip install -r requirements.txt
  python main.py                          # full pipeline, one command
  # open reports/pipeline_report.html in a browser

  # 2. Web Dashboard (PRIMARY INTERFACE)
  cd ../aviation-safety-app
  # Backend
  cd backend && python -m venv venv && source venv/bin/activate
  pip install -r requirements.txt
  python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords'); nltk.download('wordnet'); nltk.download('punkt_tab')"
  uvicorn main:app --reload --host 0.0.0.0 --port 8000

  # Frontend (separate terminal)
  cd frontend && npm install && npm run dev
  # http://localhost:5173

  # 3. Legacy Streamlit dashboard (optional)
  cd ../safety_nlp_pipeline
  streamlit run app_streamlit.py          # http://localhost:8501

  # 4. Legacy Textual terminal UI (optional)
  python app.py                           # numbered menu

  # 5. Real-time Monitor (optional)
  python -m src.monitor                   # continuous incident monitoring

First run auto-downloads NLTK corpora and live datasets (2-5 minutes).
Subsequent runs reuse cache. Monitor requires trained model - run main.py first.

-------------------------------------------------------------------------------
12. RESULTS
-------------------------------------------------------------------------------
  - Dataset : ~3,700 cleaned reports — 2,000 NASA ASRS aviation narratives
              (expert anomaly labels) + ~1,700 power-grid narratives extracted
              from 12 public NERC event-analysis PDFs.
  - Model   : ~70% weighted F1 / ~71% accuracy over 28 classes with TF-IDF +
              SGD/log-loss and GridSearchCV (best: alpha=1e-4, balanced class
              weights, CV F1 0.690). Aviation classes are noisy and
              imbalanced; power-grid classes like 'Northeast Blackout 2003'
              are near-perfect (F1 0.98).
  - RAG     : aviation queries predict ATC/conflict categories with aviation
              evidence; power-grid queries predict NERC event categories with
              grid-incident evidence — every prediction is auditable.
  - Outputs : reports/pipeline_report.html (self-contained) + Web Dashboard;
              all metrics, plots and examples accessible via browser.

-------------------------------------------------------------------------------
13. RESEARCH GAPS ADDRESSED
-------------------------------------------------------------------------------
  - Gap 4 (black-box provenance): RAG explainability module provides an audit
    trail — the report and dashboard show which historical reports influenced
    each prediction.
  - Gap 2 (edge deployment): the lightweight TF-IDF + SGD model is far smaller
    than LLMs and deploys to constrained devices easily.
  - Gap 3 (cross-domain taxonomy): aviation and power-grid narratives share one
    TF-IDF vocabulary, so the model distinguishes domains while RAG evidence
    surfaces genuine cross-domain similarities when they exist.
  - Gap 1 (physical constraints): the modular design allows swapping the
    classifier output for a solver-based constraint verifier.

-------------------------------------------------------------------------------
14. GIT HISTORY (most recent first)
-------------------------------------------------------------------------------
  6e3d442 feat: Enhance AssistantPage with Ollama integration and chat functionality
  c4ca950 feat: improve command execution and backend startup messages in launcher script
  08a4ac0 feat: add one-click launcher scripts for Safety NLP Pipeline in Python, PowerShell, Bash, and Batch
  2bb1761 feat: add column aliases for dataset normalization and update ML service to use them
  11c73d7 feat: add endpoint and UI for loading ML artifacts with status indicators
  3e4bf5d feat(ui): create index file to export UI components
  437b774 feat: Enhance SystemPage with pipeline progress tracking and UI improvements
  069362d feat: add Performance, RAG, and System pages with UI components and state management
  52ec4ba refactor: streamline process management UI and enhance service descriptions
  2166704 feat: enhance Streamlit dashboard with process management and additional tabs
  f8d9a19 Add real-time incident monitoring and alerting feature
  1941b0e Refactor project overview and reporting structure: transition from terminal UI to web dashboard
  efb0e39 feat: add Streamlit web dashboard for Safety NLP Pipeline with interactive data insights
  0caafdc Add Safety NLP Pipeline components and requirements
  91d65bc Refactor Aviation NLP Pipeline to Support Power-Grid Safety Data
  821104b Add research document, tables extract, and report generation script
  c7454b0 Enhance Aviation NLP Pipeline with Menu-Driven TUI and Data Assistant
  8f88efc feat: implement complete aviation NLP pipeline with terminal UI and data processing steps
  95ff3a6 Refactor code structure for improved readability and maintainability
  e0b01ca feat: implement RAG semantic retrieval and explainable classification output
  9322ff0 chore: initialize project workspace and gitignore

-------------------------------------------------------------------------------
15. EXTRAS
-------------------------------------------------------------------------------
  - make_report.py : script that builds DOCX report
    (Aviation_NLP_Pipeline_Report.docx) containing project summary, updated
    overview, architecture, structure, dependencies, latest run metrics and the
    FULL source code of every file.
  - Research document : "AI Applications in Aerospace and Power Infrastructure
    Safety" (DOCX) with _research_extract.txt and _tables_extract.txt
    text/tables extracts — supporting research material for the project.

-------------------------------------------------------------------------------
16. REAL-TIME MONITORING & ALERTING (src/monitor.py)
-------------------------------------------------------------------------------
The batch pipeline only reacts when run. The monitor makes it PROACTIVE:
ingests new incident reports as they arrive, classifies them on-the-fly,
scores their risk level and raises alerts with RAG evidence from similar past
incidents.

INGESTION SOURCES (all fault-tolerant):
  1. Drop-in folder  : any new_incidents/*.csv or *.txt file
  2. Master dataset  : rows appended to data/real_safety_dataset.csv
  3. NTSB API        : public US aviation accident feed (daily)
  4. UKPN Live Faults: near-real-time UK power cuts (minute-level, ≥100 customers → high)

RISK ASSESSMENT:
  critical -> fire, smoke, loss of communication, power outage, crash, emergency, terrain, engine failure...
  high     -> altitude, runway, weather, wind shear, engine, fuel, outage, storm, arctic...
  medium   -> everything else.

ALERT LIFECYCLE:
  new report -> classify -> assess risk -> if critical/high: build result -> append to data/alerts.csv -> shown in Dashboard Live Alerts tab with evidence.

RELIABILITY:
  - Lazy cached artifact loading
  - De-duplication state in data/monitor_state.json (survives restarts)
  - Per-item try/except (one bad incident never stops the loop)
  - Independent poll cadences (NTSB hourly, UKPN minute)

RUN:
  python main.py --monitor              # train, then start monitoring
  python -m src.monitor                 # standalone monitor loop
  python -m src.monitor --once --no-api # single scan, no live feeds

FUTURE: Kafka/RabbitMQ subscriber; email/SMS notifications; recommendation engine; incremental learning.

================================================================================
END OF PROJECT OVERVIEW
================================================================================